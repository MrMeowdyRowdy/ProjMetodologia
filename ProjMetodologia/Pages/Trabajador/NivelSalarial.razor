@using System.Net.Http.Json
@using Data
@using System.Text
@using System.Text.Json
@using Data.Trabajador;

@page "/NivelSalarialTrabajador"
@inject HttpClient Http


<h3>Tipo de cuenta</h3>

@if (nivelSalarialTrabajador != null)
{
    <table class="table">
        <thead>
            <tr>
                <th class="white-text">Codigo</th>
                <th class="white-text">Descripcion</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var atributo in nivelSalarialTrabajador)
            {
                <tr>
                    <td class="white-text">@atributo.Codigo</td>
                    <td class="white-text">@atributo.Descripcion</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Loading...</p>
}

@code {
    private List<AtributoSalario> nivelSalarialTrabajador = new List<AtributoSalario>();

    private async Task GetTrabajadorData()
    {
        var apiName = $"api/Varios/NivelSalarial";

        try
        {
            nivelSalarialTrabajador = await Http.GetFromJsonAsync<List<AtributoSalario>>(apiName);
        }
        catch (HttpRequestException e)
        {
            Console.WriteLine($"Error al obtener datos del usuario: {e.Message}");
        }
    }

    protected override async Task OnInitializedAsync()
    {
        await GetTrabajadorData();
    }
}
