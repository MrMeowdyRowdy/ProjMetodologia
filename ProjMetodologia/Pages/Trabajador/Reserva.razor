@using System.Net.Http.Json
@using Data
@using System.Text
@using System.Text.Json
@using Data.Trabajador;

@page "/ReservaTrabajador"
@inject HttpClient Http


<h3>Fondo reserva</h3>

@if (reservaTrabajador != null)
{
    <table class="table">
        <thead>
            <tr>
                <th class="white-text">Codigo</th>
                <th class="white-text">Descripcion</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var atributo in reservaTrabajador)
            {
                <tr>
                    <td class="white-text">@atributo.Codigo</td>
                    <td class="white-text">@atributo.Descripcion</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Loading...</p>
}

@code {
    private List<AtributoTrabajador> reservaTrabajador = new List<AtributoTrabajador>();

    private async Task GetTrabajadorData()
    {
        var apiName = $"api/Varios/FondoReserva";

        try
        {
            reservaTrabajador = await Http.GetFromJsonAsync<List<AtributoTrabajador>>(apiName);
        }
        catch (HttpRequestException e)
        {
            Console.WriteLine($"Error al obtener datos del usuario: {e.Message}");
        }
    }

    protected override async Task OnInitializedAsync()
    {
        await GetTrabajadorData();
    }
}
